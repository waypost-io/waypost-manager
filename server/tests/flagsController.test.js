const { getAllFlags, getFlag, createFlag, editFlag, deleteFlag } = require('../controllers/flagsController');

describe('Test flag controller', () => {
  const noop = () => {};

  beforeEach(() => {});

  afterEach(() => {});

  test('getAllFlags returns all the flags', async () => {
    // test getAllFlags
  });

  test('getFlag returns the flag by id', async () => {

  });

  test('createFlag creates a new flag and returns it', async () => {

  });

  test('editFlag updates a flag and returns updated flag', async () => {

  });

  test('deleteFlag sets the is_deleted field to true', async () => {

  });
});